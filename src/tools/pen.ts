// Pen tool - handled directly by AnnotationCanvas + DrawingEngine
// This module exists for future extensibility
export const PEN_DEFAULTS = {
	thinning: 0.5,
	smoothing: 0.5,
	streamline: 0.5,
} as const;
