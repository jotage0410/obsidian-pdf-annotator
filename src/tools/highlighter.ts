// Highlighter tool - handled directly by DrawingEngine with globalAlpha
// This module exists for future extensibility
export const HIGHLIGHTER_DEFAULTS = {
	opacity: 0.3,
	defaultWidth: 20,
} as const;
